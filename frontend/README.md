
  # Convert design to React code

  This is a code bundle for Convert design to React code. The original project is available at https://www.figma.com/design/tGloP20NBI1QLICw3AzxNX/Convert-design-to-React-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  